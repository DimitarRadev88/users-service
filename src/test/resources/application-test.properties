spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
sprint.datasource.name="test-user-db"
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql = true

sql.script.create.user=INSERT INTO users (username, email, password) \
  VALUES ('new-user', 'new-user-email@email.com', 'new-user-password');
sql.script.create.role=INSERT INTO roles (role_type) \
  VALUES ('USER');
sql.script.create.users_roles=INSERT INTO users_roles (user_id, role_id) VALUES (1, 1);
sql.script.delete.users_roles=DELETE FROM users_roles;
sql.script.delete.user=DELETE FROM users;
sql.script.delete.role=DELETE FROM roles;
sql.script.create.user.role=INSERT INTO users_roles (user_id, role_id) \
   VALUES (4, 2);
sql.script.create.password_reset_token=INSERT INTO 'workout-scheduler-users-db'.password_reset_token ('token', 'user_id', 'expiration_date') \
   VALUES('password_reset_token',1, CURRENT_TIMESTAMP + INTERVAL 1 HOUR);
